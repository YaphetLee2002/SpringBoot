spring:
  profiles:
    active: test

---

server:
  port: 80
spring:
  config:
    activate:
      on-profile: dev
---

server:
  port: 81
spring:
  config:
    activate:
      on-profile: pro
---

server:
  port: 82
spring:
  config:
    activate:
      on-profile: test